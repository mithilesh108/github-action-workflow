name: Greetings workflow_dispatch
on:
  workflow_dispatch:
    input:
      name: env
jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Greetings
        run: echo "Hello Github Actions!"
